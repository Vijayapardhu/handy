# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml

# Android
android/.gradle/
android/local.properties
android/key.properties
android/app/upload-keystore.jks

# Firebase config generated by flutterfire configure.
# google-services.json holds only the public Android client config (like the
# web app's VITE_FIREBASE_* values) — kept in the repo so the app builds from a
# clean checkout.
